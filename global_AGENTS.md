- Commit to Git after changes: Start the commit message with a short summary of the change, focusing on intent and effect. Add helpful context as needed (the user's request, background, approach, rationale, etc.). E.g., `git add <files> && git commit -m "<summary>" -m $'- <msg1>\n- <msg2>'`
- After making changes, update `AGENTS.md` if it exists and is outdated.
